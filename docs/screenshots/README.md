# Screenshots

Expected files:
- `01-home-input.png`
- `02-prediction-output.png`

Generate automatically with:

```bash
python scripts/capture_screenshots.py
```

If the script cannot run in your environment, capture these two pages manually from the running Flask app and save them with the exact names above.
